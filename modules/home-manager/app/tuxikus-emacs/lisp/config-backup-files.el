;; config-backup-files.el --- -*- lexical-binding: t -*-

;; disable backup files (files with ~)
(setq make-backup-files nil)
;; disable lock files (files with #)
(setq create-lockfiles nil)

(provide 'config-backup-files)

;; config-backup-files.el ends here
