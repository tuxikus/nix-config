;; config-emms.el --- -*- lexical-binding: t -*-

(emms-all)
(setq emms-player-list '(emms-player-mpv)
      emms-info-functions '(emms-info-native))

(provide 'config-emms)

;; config-emms.el ends here
